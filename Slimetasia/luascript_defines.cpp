#include "Input.h"
#include "RigidbodyComponent.h"
#include "luascript.h"

void LuaScript::load_defines(lua_State* L)
{
    lua_setConstant(L, MOUSE_LEFT);
    lua_setConstant(L, MOUSE_RIGHT);
    lua_setConstant(L, MOUSE_MID);
    lua_setConstant(L, KEY_BACK);
    lua_setConstant(L, KEY_TAB);
    lua_setConstant(L, KEY_CLEAR);
    lua_setConstant(L, KEY_RETURN);
    lua_setConstant(L, KEY_LSHIFT);
    lua_setConstant(L, KEY_RSHIFT);
    lua_setConstant(L, KEY_LCTRL);
    lua_setConstant(L, KEY_RCTRL);
    lua_setConstant(L, KEY_CAPS);
    lua_setConstant(L, KEY_ESCAPE);
    lua_setConstant(L, KEY_NEXT);
    lua_setConstant(L, KEY_END);
    lua_setConstant(L, KEY_HOME);
    lua_setConstant(L, KEY_LEFT);
    lua_setConstant(L, KEY_UP);
    lua_setConstant(L, KEY_RIGHT);
    lua_setConstant(L, KEY_DOWN);
    lua_setConstant(L, KEY_INSERT);
    lua_setConstant(L, KEY_DELETE);
    lua_setConstant(L, KEY_SPACE);
    lua_setConstant(L, KEY_0);
    lua_setConstant(L, KEY_1);
    lua_setConstant(L, KEY_2);
    lua_setConstant(L, KEY_3);
    lua_setConstant(L, KEY_4);
    lua_setConstant(L, KEY_5);
    lua_setConstant(L, KEY_6);
    lua_setConstant(L, KEY_7);
    lua_setConstant(L, KEY_8);
    lua_setConstant(L, KEY_9);
    lua_setConstant(L, KEY_A);
    lua_setConstant(L, KEY_B);
    lua_setConstant(L, KEY_C);
    lua_setConstant(L, KEY_D);
    lua_setConstant(L, KEY_E);
    lua_setConstant(L, KEY_F);
    lua_setConstant(L, KEY_G);
    lua_setConstant(L, KEY_H);
    lua_setConstant(L, KEY_I);
    lua_setConstant(L, KEY_J);
    lua_setConstant(L, KEY_K);
    lua_setConstant(L, KEY_L);
    lua_setConstant(L, KEY_M);
    lua_setConstant(L, KEY_N);
    lua_setConstant(L, KEY_O);
    lua_setConstant(L, KEY_P);
    lua_setConstant(L, KEY_Q);
    lua_setConstant(L, KEY_R);
    lua_setConstant(L, KEY_S);
    lua_setConstant(L, KEY_T);
    lua_setConstant(L, KEY_U);
    lua_setConstant(L, KEY_V);
    lua_setConstant(L, KEY_W);
    lua_setConstant(L, KEY_X);
    lua_setConstant(L, KEY_Y);
    lua_setConstant(L, KEY_Z);
    lua_setConstant(L, KEY_NUM_0);
    lua_setConstant(L, KEY_NUM_1);
    lua_setConstant(L, KEY_NUM_2);
    lua_setConstant(L, KEY_NUM_3);
    lua_setConstant(L, KEY_NUM_4);
    lua_setConstant(L, KEY_NUM_5);
    lua_setConstant(L, KEY_NUM_6);
    lua_setConstant(L, KEY_NUM_7);
    lua_setConstant(L, KEY_NUM_8);
    lua_setConstant(L, KEY_NUM_9);
    lua_setConstant(L, KEY_MUL);
    lua_setConstant(L, KEY_ADD);
    lua_setConstant(L, KEY_SEP);
    lua_setConstant(L, KEY_SUB);
    lua_setConstant(L, KEY_DOT);
    lua_setConstant(L, KEY_DIV);
    lua_setConstant(L, KEY_F1);
    lua_setConstant(L, KEY_F2);
    lua_setConstant(L, KEY_F3);
    lua_setConstant(L, KEY_F4);
    lua_setConstant(L, KEY_F5);
    lua_setConstant(L, KEY_F6);
    lua_setConstant(L, KEY_F7);
    lua_setConstant(L, KEY_F8);
    lua_setConstant(L, KEY_F9);
    lua_setConstant(L, KEY_F10);
    lua_setConstant(L, KEY_F11);
    lua_setConstant(L, KEY_F12);
    lua_setConstant(L, GAMEPAD_DPAD_UP);
    lua_setConstant(L, GAMEPAD_DPAD_DOWN);
    lua_setConstant(L, GAMEPAD_DPAD_LEFT);
    lua_setConstant(L, GAMEPAD_DPAD_RIGHT);
    lua_setConstant(L, GAMEPAD_START);
    lua_setConstant(L, GAMEPAD_BACK);
    lua_setConstant(L, GAMEPAD_LS);
    lua_setConstant(L, GAMEPAD_RS);
    lua_setConstant(L, GAMEPAD_LB);
    lua_setConstant(L, GAMEPAD_RB);
    lua_setConstant(L, GAMEPAD_A);
    lua_setConstant(L, GAMEPAD_B);
    lua_setConstant(L, GAMEPAD_X);
    lua_setConstant(L, GAMEPAD_Y);
}
