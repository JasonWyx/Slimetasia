#include "PhysicsWorldSettings.h"

#include "ResourceManager.h"

void PhysicsWorldSettings::LoadFromFile(std::string filename)
{
    // ResourceManager::Instance().LoadPhysicsWorldSettings(filename);
}

void PhysicsWorldSettings::SaveToFile(std::string filename) const
{
    // ResourceManager::Instance().SavePhysicsWorldSettings(filename);
}
